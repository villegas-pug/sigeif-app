spring:
   cloud:
      gateway:
         globalcors:
            cors-configurations:
               '[/**]':
                  allowedOriginPatterns: ${app.security.allowed-origins}
                  allowedMethods: "*"
                  allowedHeaders: "*"
                  allowCredentials: true
                  exposedHeaders:
                     - "Content-Disposition"